 echo "STATS;" | ./iosh | awk '
	BEGIN {n="unknown"; s="unknown"; }
	$1 ~ /name/{ n=$2} 
	$1 ~ /statistic/{s=$2} 
	$1 ~ /sum/ { stats[n] = $2 }
	END{for(i in stats) { 
			for (j in stats[i]) printf "%s %s %s\n",i, j, stats[i][j]
		}
	}
	'
